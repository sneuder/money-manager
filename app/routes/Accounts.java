package routes;

import server.Server;
import controllers.Accounts.*;

public class Accounts {
  static public void route() {
    // Server.server.createContext("/accounts/get", new GetAccounts());
    // Server.server.createContext("/account/get", new GetAccount());
    // Server.server.createContext("/account/post", new PostAccount());
    // Server.server.createContext("/account/update", new UpdateAccount());
    // Server.server.createContext("/account/delete", new DeleteAccount());
  }
}
